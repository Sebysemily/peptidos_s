name: amptox
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - pandas
  - numpy
  - scikit-learn=1.0.2  # often required for old pickle models
  - pip
  - pip:
    - propy3
    - isoelectric
